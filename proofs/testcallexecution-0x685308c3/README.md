# TestCallExecution (Ethereum Alarm Clock test contract)

- **Address:** `0x685308c340f91faea1b9263b2ebb9e71fc9a751d`
- **Deployer:** `0xd3cda913deb6f67967b99d67acdfa1712c293601` (Piper Merriam)
- **Deployment tx:** `0xe9554c1cc9f89d50a4384d291b6a9949af6ea49c8b5b301a6e236f553139c03d`
- **Block:** 737,976 (2015-12-23 22:48:08 UTC)
- **Runtime size:** 4,005 bytes
- **Balance (May 2026):** 4.896 ETH
- **Verification status:** author-published source, not a byte-for-byte compiler match

## Identification

All 33 function selectors in the deployed runtime resolve cleanly against the ABI of `TestCallExecution` in Piper Merriam's `pipermerriam/ethereum-alarm-canary` repo (and matching ABI in `pipermerriam/ethereum-alarm-client`):

- `wasSuccessful()` `reset()` `setBool()` `setUInt(uint256)` `setInt(int256)` `setAddress(address)` `setBytes32(bytes32)` `setBytes(bytes)` `setMany(uint256,int256,uint256,bytes20,address,bytes)`
- `doExecution(address)` `doLoops(uint256)`
- `registerBool/UInt/Int/Bytes/Bytes32/Address(address, value)` `registerData(address,int,bytes32,address)` `registerMany(address,uint,int,uint,bytes20,address,bytes)`
- `scheduleSetBool(address,uint256,bool)` `scheduleSetUInt(address,uint256,uint256)`
- plus 12 storage getters auto-generated by Solidity (`v_bool`, `v_uint`, `v_int`, `v_address`, `v_bytes32`, `v_bytes`, `vm_a`, `vm_b`, `vm_c`, `vm_d`, `vm_e`, `vm_f`)

The published `TestCallExecution` in tagged releases `v0.5.0`/`v0.6.0` of `pipermerriam/ethereum-alarm-clock` covers the `set*` / `do*` / register family. The `scheduleSet*` helpers are present in the on-chain bytecode but not in any tagged source release - they were almost certainly in an untagged commit around the deployment date (Dec 2015), since the released versions tagged later evolved into a different testing structure.

This deployment predates the first tagged release (v0.1.0) by ~3 weeks and is one of Piper's earliest on-chain experiments for the Alarm Clock infrastructure.

## Why it holds 4.9 ETH

The contract's `doLoops(uint iterations)` repeatedly sends 1 wei back to itself via `address(this).send(1)` to test the scheduler's ability to handle nested calls. Funds accumulated during test runs and were never withdrawn (the contract has no `withdraw` function).

## Files

- `Testers_v0.6.0.sol` - the closest published version of `TestCallExecution` (in `contracts/Testers.sol` at tag v0.6.0)
- `Testers_v0.1.0.sol` - the v0.1.0 Testers.sol for completeness (does not contain TestCallExecution yet)

## References

- Repo: https://github.com/pipermerriam/ethereum-alarm-clock
- Canary build: https://github.com/pipermerriam/ethereum-alarm-canary/blob/master/canary/versions/v0.6/build/contracts.json
- Sibling contract `0x6c8f2a135f6ed072de4503bd7c4999a1a17f824b` (`Scheduler`) is etherscan-verified.
